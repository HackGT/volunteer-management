import React, { useState } from 'react'

const ClockIn = () => {
    return(
        <div className="App">
            <p>(Name) clocked in @ (time)</p>
        </div>

    )
}

export default ClockIn;